---
const galleryImages = [
    { src: '/images/gallery-dish-1.jpg', alt: 'Gourmet főétel' },
    { src: '/images/gallery-interior-1.jpg', alt: 'Étterem belső tér' },
    { src: '/images/gallery-wine.jpg', alt: 'Prémium Borok' },
    { src: '/images/gallery-dish-2.jpg', alt: 'Elegáns előétel' },
    { src: '/images/gallery-dish-3.jpg', alt: 'Filet Mignon' },
    { src: '/images/gallery-dish-4.jpg', alt: 'Szent Jakab-kagyló' },
    { src: '/images/gallery-dish-5.jpg', alt: 'Tatár beefsteak' },
    { src: '/images/gallery-terrace.jpg', alt: 'Kültéri terasz' },
    { src: '/images/gallery-dessert.jpg', alt: 'Édes desszert' },
];
---

<section id="gallery">
    <div class="container" data-animate-on-scroll>
        <h2>Galéria</h2>
        <p class="section-subtitle">Engedje, hogy képeink meséljenek éttermünk hangulatáról, ételeinkről és a nálunk töltött felejthetetlen pillanatokról.</p>
        <div class="gallery-grid">
            {galleryImages.map(image => (
                <div class="gallery-item">
                    <img src={image.src} alt={image.alt} loading="lazy">
                    <div class="overlay-text">{image.alt}</div>
                </div>
            ))}
        </div>
    </div>
</section>

<style>
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    max-width: 1000px;
    margin: 0 auto;
  }
  .gallery-item {
    position: relative;
    overflow: hidden;
    border-radius: 8px;
    height: 280px; /* Fix magasság a képeknek */
    box-shadow: 0 4px 15px rgba(0,0,0,0.4);
    cursor: pointer;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  .gallery-item:hover {
    transform: translateY(-8px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.6);
  }
  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Fontos, hogy a képek kitöltsék a fix méretet */
    transition: transform 0.4s ease-out, filter 0.4s ease-out;
  }
  .gallery-item:hover img {
    transform: scale(1.08);
    filter: brightness(0.8);
  }
  .overlay-text {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 1rem;
    background: linear-gradient(to top, rgba(0,0,0,0.8), rgba(0,0,0,0));
    color: white;
    font-family: var(--font-serif);
    font-size: 1.2rem;
    font-weight: 600;
    text-align: center;
    opacity: 0;
    transform: translateY(100%);
    transition: opacity 0.4s ease-out, transform 0.4s ease-out;
  }
  .gallery-item:hover .overlay-text {
      opacity: 1;
      transform: translateY(0);
  }
  
  @media (max-width: 768px) {
      .gallery-grid {
          grid-template-columns: 1fr;
      }
      .gallery-item {
          height: 250px;
      }
  }
</style>